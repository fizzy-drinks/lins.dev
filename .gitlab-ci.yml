image: node:latest

before_script:
  - npm i -g yarn
  - yarn install

pages:
  stage: deploy
  script:
    - yarn gitlab
    - cp favicon.ico public/favicon.ico
    - cp sitemap.xml public/sitemap.xml
  artifacts:
    paths:
      - public
  only:
    - master
