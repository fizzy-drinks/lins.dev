image: node:latest

before_script:
  - npm i

pages:
  stage: deploy
  script:
    - npm run gitlab
    - cp favicon.ico public/favicon.ico
  artifacts:
    paths:
      - public
  only:
    - master
